import { TaskFilterPipeTsPipe } from './task-filter.pipe.ts-pipe';

describe('TaskFilterPipeTsPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskFilterPipeTsPipe();
    expect(pipe).toBeTruthy();
  });
});
